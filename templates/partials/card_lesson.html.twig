<div class="card-lesson p-3 mt-3">
    <div class="d-flex justify-content-between">
        <div>
            <h4>{{ lesson.title }}</h4>
            <h5>{{ lesson.subject.title }}</h5>
        </div>
        <div>
            <span class="date-created">créé le {{ lesson.dateCreated|date('d/m/Y H:i:s') }}</span>
        </div>
    </div>
    <div class="mt-4">
        <p>{{ lesson.description }}</p>
    </div>
    <div class="d-flex justify-content-center mt-4">
        <img src="{{ asset('build/images/caret-down-dark.png') }}" alt="">
    </div>
</div>